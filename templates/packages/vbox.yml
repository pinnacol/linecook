# Configure the package.
linecook:
  package:
    recipes:
    - <%= project_name %>
    - <%= project_name %>_test

# Attributes set here override those in the attributes directory.
<%= project_name %>:
  numbers: [7, 8, 9]
